# Unity Smart Merge (UnityYAMLMerge) は衝突時に意図せずリモート側を残すことがあったため無効化。
# Unity 専用 YAML ファイルは通常の git テキストマージに任せる。
# 一般的なバイナリ: マージ衝突時は手動解決
*.fbx     binary
*.png     binary
*.jpg     binary
*.jpeg    binary
*.tga     binary
*.psd     binary
*.tif     binary
*.exr     binary
*.hdr     binary
*.wav     binary
*.mp3     binary
*.ogg     binary
*.dll     binary
Assets/Fonts/Zen_Old_Mincho/ZenOldMincho-Bold[[:space:]]SDF.asset filter=lfs diff=lfs merge=lfs -text
